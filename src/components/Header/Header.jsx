import React from 'react';
import classes from './Header.module.css';

const Header = () => {
    return (
        <header className={classes.header}>
            <img src="https://www.quizony.com/company-name-generator/imageForSharing.jpg"></img>
        </header>
    );
}

export default Header;